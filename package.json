{
  "name": "warrior_timer",
  "version": "1.0.0",
  "description": "A gym interval timer with URL-based interval definitions. Create custom workout timers by defining intervals directly in the URL.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./app/assets/stylesheets/application.tailwind.css -o ./app/assets/builds/application.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  }
}
