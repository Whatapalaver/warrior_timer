<div
  data-controller="workout-storage"
  class="space-y-16"
>
  <!-- Favorites Section -->
  <div>
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-3xl font-bold text-amber-400">★ Favorites</h2>
      <button
        data-action="click->workout-storage#clearFavorites"
        class="text-sm text-slate-400 hover:text-slate-300 transition-colors"
      >
        Clear All
      </button>
    </div>

    <div data-workout-storage-target="favoritesList" class="grid md:grid-cols-2 gap-4">
      <!-- Populated by JavaScript -->
      <div class="col-span-2 text-center py-12 text-slate-400 text-sm">
        <p class="text-lg">No favorites yet</p>
        <p class="text-xs mt-2">Click the ★ button on any timer page to save it</p>
      </div>
    </div>
  </div>

  <!-- Recent Workouts Section -->
  <div>
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-3xl font-bold text-emerald-400">🕐 Recent Workouts</h2>
      <button
        data-action="click->workout-storage#clearRecents"
        class="text-sm text-slate-400 hover:text-slate-300 transition-colors"
      >
        Clear All
      </button>
    </div>

    <div data-workout-storage-target="recentsList" class="grid md:grid-cols-2 gap-4">
      <!-- Populated by JavaScript -->
      <div class="col-span-2 text-center py-12 text-slate-400 text-sm">
        <p class="text-lg">No recent workouts</p>
        <p class="text-xs mt-2">Your recently used timers will appear here</p>
      </div>
    </div>
  </div>
</div>
